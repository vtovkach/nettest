config:
  protocol: "udp"
  family: "ipv6"
  ip_address: "::1"
  port: "10003"

tests:
  - test:
      steps:
        - send: "connect"
          expect: "connected"

        - send: "login user"
          expect: "login ok"

        - send: "quit"
          expect: "disconnected"

  - test:
      steps:
        - send: "health"
          expect: "healthy"