config:
  protocol: "udp"
  family: "ipv6"
  ip_address: "::1"
  port: "10003"

tests:
  - name: "multiple step conversation"
    steps:
      - send: "connect"
        expect: "connected"

      - send: "login user"
        expect: "login ok"

      - send: "quit"
        expect: "disconnected"

  - name: "second session"
    steps:
      - send: "health"
        expect: "healthy"